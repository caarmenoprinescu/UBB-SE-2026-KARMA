<Page
    x:Class="KarmaBanking.App.Views.LoansView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Padding="20">

            <TextBlock Text="Loans Dashboard"
                       FontSize="28"
                       FontWeight="Bold"
                       Margin="0,0,0,20"/>

            <ListView ItemsSource="{Binding loans}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray"
                                BorderThickness="1"
                                CornerRadius="8"
                                Margin="0,0,0,15"
                                Padding="15">

                            <StackPanel>

                                <TextBlock Text="{Binding loanType}"
                                           FontSize="20"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,10"/>

                                <TextBlock>
                                    <Run Text="Principal: "/>
                                    <Run Text="{Binding principal}"/>
                                </TextBlock>

                                <TextBlock>
                                    <Run Text="Balance: "/>
                                    <Run Text="{Binding outstandingBalance}"/>
                                </TextBlock>

                                <TextBlock>
                                    <Run Text="Interest: "/>
                                    <Run Text="{Binding interestRate}"/>
                                    <Run Text="%"/>
                                </TextBlock>

                                <TextBlock>
                                    <Run Text="Monthly: "/>
                                    <Run Text="{Binding monthlyInstallment}"/>
                                </TextBlock>

                                <TextBlock>
                                    <Run Text="Remaining months: "/>
                                    <Run Text="{Binding remainingMonths}"/>
                                </TextBlock>

                                <TextBlock Margin="0,5,0,0">
                                    <Run Text="Progress: "/>
                                    <Run Text="{Binding repaymentProgress}"/>
                                    <Run Text="%"/>
                                </TextBlock>

                                <ProgressBar Value="{Binding repaymentProgress}"
                                             Maximum="100"
                                             Height="10"
                                             Margin="0,5,0,10"/>

                                <StackPanel Orientation="Horizontal">

                                    <Button Content="Schedule"
                                            Click="Schedule_Click"
                                            Tag="{Binding id}"
                                            Margin="0,0,10,0"/>

                                    <Button Content="Pay Installment"
                                            Click="Pay_Click"
                                            Tag="{Binding id}"/>

                                </StackPanel>

                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackPanel>
    </ScrollViewer>
</Page>