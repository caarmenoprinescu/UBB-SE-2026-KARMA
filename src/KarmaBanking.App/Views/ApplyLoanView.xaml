<Page
    x:Class="KarmaBanking.App.Views.ApplyLoanView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Padding="20" Spacing="10">

            <TextBlock Text="Apply for Loan"
                       FontSize="28"
                       FontWeight="Bold"/>

            <ComboBox Header="Loan Type"
                      ItemsSource="{Binding LoanTypes}"
                      SelectedItem="{Binding selectedLoanType, Mode=TwoWay}"/>

            <TextBox Header="Amount"
                     Text="{Binding desiredAmount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <ComboBox Header="Term (months)"
                      ItemsSource="{Binding AvailableTerms}"
                      SelectedItem="{Binding preferredTermMonths, Mode=TwoWay}"/>

            <TextBox Header="Purpose"
                     Text="{Binding purpose, Mode=TwoWay}"/>

            <TextBlock Text="Estimate"
                       FontSize="18"
                       Margin="0,10,0,0"/>

            <TextBlock Text="{Binding currentEstimate.IndicativeRate}"/>
            <TextBlock Text="{Binding currentEstimate.MonthlyInstallment}"/>
            <TextBlock Text="{Binding currentEstimate.TotalRepayable}"/>

            <Button Content="Submit"
                    Click="Submit_Click"
                    Margin="0,10,0,0"/>

            <TextBlock Text="{Binding statusMessage}"
                       Foreground="Green"/>

        </StackPanel>
    </ScrollViewer>
</Page>